services:
  test-db:
    image: postgres:17
    environment:
      POSTGRES_DB: restart_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5435:5432"
    tmpfs:
      - /var/lib/postgresql/data
