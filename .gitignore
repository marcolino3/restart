# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup

# OS
.DS_Store

# IDE
.idea/

# Claude Code (local-only files; tracked agents/commands remain checked in)
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Dependencies (hoisted via pnpm workspaces)
node_modules/

# Build outputs
dist/
.next/
coverage/
out/

# Turborepo
.turbo/

# Environment files
.env
.env.*
!.env.example

# Sealed-Secrets — NUR die verschlüsselten SealedSecret-Manifeste sind im Git
# zugelassen. Klartext-Secret-Manifeste, Master-Key-Backups und temporäre
# Output-Files niemals committen.
*.secret.yaml
secrets-plain.yaml
sealed-secrets-backup/
sealed-secrets-keys-*.yaml
sealed-secrets-keys-*.yaml.age
