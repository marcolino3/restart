# Old separate repos (kept on disk, not part of monorepo)
restart-backend/
restart-ui/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup

# OS
.DS_Store

# IDE
.idea/

# Dependencies (each app has its own)
node_modules/

# Environment files
.env
.env.*
!.env.example
