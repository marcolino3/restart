{
  "name": "@restart/shared-auth-client",
  "version": "0.0.1",
  "private": true,
  "description": "Auth constants and session types shared by web (Next.js) and mobile (Expo) apps. Each app instantiates its own better-auth client (Cookie vs SecureStore).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ]
}
